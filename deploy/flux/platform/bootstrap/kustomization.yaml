# Applied ONCE PER CLUSTER, by hand, by somebody with admin on it:
#
#   kubectl apply -k deploy/flux/platform/bootstrap
#
# Everything after that is a pull request.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gitrepository.yaml
  - kustomization-platform.yaml
