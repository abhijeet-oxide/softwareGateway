{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    // For the files Vite's native config loader reads, which resolve no
    // extension of their own (see vite.config.ts). Safe only because nothing
    // here is emitted - TypeScript requires noEmit for it.
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": false,
    // TypeScript 7 removed baseUrl, so the mapping is relative to this file.
    "paths": { "@/*": ["./src/*"] },
    "types": ["unplugin-icons/types/react"]
  },
  "include": ["src"]
}
