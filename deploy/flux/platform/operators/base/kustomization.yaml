apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # The operator only. Its chart source is a flux-system object and lives with
  # the other sources in ../../bootstrap - see the comment in that file.
  - cloudnative-pg.yaml
