apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# WHICH RELEASE OF THE SOFTWARE THIS INSTANCE RUNS. One line, and moving it is
# both a deploy and a rollback.
resources:
  - ../../../software/patch/0.1.0