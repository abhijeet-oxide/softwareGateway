# Applied ONCE PER CLUSTER, by hand, by somebody with admin on it:
#
#   kubectl apply -k deploy/flux/platform/bootstrap/namespace-scoped/lab
#
# Everything after that is a pull request. See ../../operators/README.md for
# which scope this cluster wants.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gitrepository.yaml
  - helmrepository.yaml
  - kustomization-platform.yaml
