// The shared data table, in one import.
//
//   import { Table } from "./tablekit";
//
// Everything in this folder is IDENTICAL in every tool that uses it, the same
// way `uikit/` is. See README.md for what may and may not be changed here.

export { Table } from "./Table";

export type {
  TableEnhancedActions,
  TableEnhancedColumn,
  TableEnhancedColumns,
  TableEnhancedComponent,
  TableEnhancedProps,
  TableEnhancedState,
} from "./Table";
