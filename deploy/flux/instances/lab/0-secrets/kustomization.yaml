apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Nothing: this instance's values set secrets.backend to `none`, so the Secrets
# it needs are created by hand. See README.md.
resources: []
